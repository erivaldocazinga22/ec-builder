{
    "name": "<%= props.name %>",
    "version": "0.0.1",
    "description": "<%= props.description %>",
    "main": "server.<%= props.extension %>",
    "license": "MIT",
    "scripts": {
        "dev": "<%= props.extension==="js" ? 'nodemon' : 'tsx --watch' %> --env-file .env src/http/server.<%=props.extension %>"
    },
    "dependencies": {},
    "devDependencies": {}
}