const path = require("path");

module.exports = {
  config: path.resolve("src", "http", "core", "config", "config.js"),
  "models-path": path.resolve("src", "http", "core", "models"),
  "seeders-path": path.resolve("src", "http", "core", "seeders"),
  "migrations-path": path.resolve("src", "http", "core", "migrations"),
};